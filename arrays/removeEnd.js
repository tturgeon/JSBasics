// Create a function called removeEnd that takes in an array and removes the
// value at the end of the array. Return the new array.

var removeEnd;

removeEnd = function(arr, num) {
  arr.pop(num);

  return arr;
}

// DO NOT DELETE
module.exports = removeEnd;
